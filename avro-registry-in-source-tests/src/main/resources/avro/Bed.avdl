@namespace("com.example")

protocol MockProtocol {

	enum BedSize {
		TWIN,
		FULL,
		QUEEN,
		KING
	}

	enum BedFirmness {
		HARD,
		SOFT
	}

	record Bed {
		BedSize size;
		BedFirmness firmness = "HARD";
	}

}
